import * as S from './styled'

export default function InputLogin(props) {
  return (
    <S.InputLogin
      name={props.name}
      placeholder={props.placeholder}
      type={props.type}
    />
  )
}
